schema_version: 1

context:
  name: PyPDFForm
  version: "4.7.10"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/chinapandaman/PyPDFForm/archive/v${{ version }}.tar.gz
  sha256: 1246ed1f667c8967720dbe67f55ea40b5a6f79c4944ed3ca6ef77d73baadbe25

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - cryptography >=46.0.3,<47.0.0
    - fonttools >=4.60.1,<5.0.0
    - pikepdf >=10.5.0,<11.0.0
    - pillow >=12.0.0,<13.0.0
    - pypdf >=6.9.0,<6.10.0
    - reportlab >=4.4.6,<5.0.0

tests:
  - python:
      imports:
        - PyPDFForm
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: The Python library for PDF forms.
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/chinapandaman/PyPDFForm

extra:
  recipe-maintainers:
    - thewchan
